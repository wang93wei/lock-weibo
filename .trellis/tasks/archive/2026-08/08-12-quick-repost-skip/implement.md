# 实施清单

1. 扩展请求头/响应解析，新增受全局限速控制的 `destroyQuickRepost(oriMid, signal)`。
2. 将扫描结果规范化为 lock/cancel/skip 动作，并覆盖 mymblog 与 searchProfile 两条预览路径。
3. 将执行器改为先锁后取消，加入分项成功统计、完成态和不可重试业务错误处理。
4. 增加默认关闭复选项、独立待取消计数、3×2 计数布局、分项预览总结和确认文案。
5. 将复选项纳入预览配置一致性检查，清空和停止行为保持现有语义。
6. 更新 API 实测记录与版本号至 `0.6.9`。
7. 运行语法、动作判据、API 载荷/分类、执行顺序/幂等/停止、版本同步和 diff 检查。
8. 人工验证清单：小范围预览；确认 `/destroy` 使用 `ori_mid`；实际取消一条快转；核对页面与总结。

## 回滚点

- 若 `/destroy` 现网行为与 bundle 不符，保留快转识别/跳过修复，关闭并移除可选取消入口，不改变锁定路径。

## 实施结果（2026-08-12）

- 已完成 userscript、面板与 API 契约记录更新，版本同步为 `0.6.9`。
- Node VM 回归覆盖默认关闭、菜单/字段兜底、缺失 `ori_mid`、普通转发、预览配置失效、
  `/destroy` JSON 载荷、先锁后取消、完成态幂等及确定性 HTTP 400 不重试。
- `node --check scripts/weibo-batch-locker.user.js`、Node VM 回归、版本同步检查和
  `git diff --check` 均通过。
- 用户已在 Tampermonkey 登录态完成生产人工验证，确认功能完全正常。
